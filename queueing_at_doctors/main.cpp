#include <bits/stdc++.h>

using namespace std;

int main()
{
    unsigned long int time, visits, counter;
    int cases, visitors, doctors;
    priority_queue
    cin >> cases;

    for (int i = 0; i < cases; i++) {
        cin >> visitors >> doctors;

        for (int visitor = 0; visitor < visitors; visitor++) {
            cin >> time >> visits;

            for (counter = 0; counter < visits; counter++) {

            }
        }

    }

    return 0;
}
